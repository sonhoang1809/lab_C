#include <stdio.h>
#include <stdlib.h>

/* tim trung binh cac so duong nhap tu ban phim, ket thuc khi go 1 so am */

int main() 
{
	int a,dem,sum;
	float tb;
	do
	(
	scanf("%d",&a);
	dem = dem+1;
	sum = sum +a;
	printf("sum = %d",sum);
	)
	while (a>=0)
	

	
	
	
	 
	
	return 0;
}
